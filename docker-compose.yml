services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      # Garante que o Flask use o PYTHONPATH correto também via Compose
      - PYTHONPATH=/app:/app/todo_project
